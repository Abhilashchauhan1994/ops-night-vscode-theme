apiVersion: apps/v1
kind: Deployment

metadata:
  name: ops-night

spec:
  replicas: 2

  template:
    spec:
      containers:
        - name: web
          image: nginx:latest

          env:
            - name: APP_ENV
              value: production